import React from 'react'

const HeaderComponent = ({appname}) => {
    return (
        <div className="header">
            <h1>{appname}</h1>
        </div>
    )
}

export default HeaderComponent
